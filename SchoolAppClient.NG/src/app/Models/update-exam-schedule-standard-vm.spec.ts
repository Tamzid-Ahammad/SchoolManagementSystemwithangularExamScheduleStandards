import { UpdateExamScheduleStandardVM } from './update-exam-schedule-standard-vm';

describe('UpdateExamScheduleStandardVM', () => {
  it('should create an instance', () => {
    expect(new UpdateExamScheduleStandardVM()).toBeTruthy();
  });
});
