import { CreateExamScheduleStandardVM } from './create-exam-schedule-standard-vm';

describe('CreateExamScheduleStandardVM', () => {
  it('should create an instance', () => {
    expect(new CreateExamScheduleStandardVM()).toBeTruthy();
  });
});
