import { CreateExamSubjectVM } from './create-exam-subject-vm';

describe('CreateExamSubjectVM', () => {
  it('should create an instance', () => {
    expect(new CreateExamSubjectVM()).toBeTruthy();
  });
});
